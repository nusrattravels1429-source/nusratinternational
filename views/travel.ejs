<%- include('partials/header', { title: 'All Travel Packages - Nusrat International', activePage: 'travel' }) %>

<!-- ================= PAGE HERO ================= -->
<div class="page-hero">
  <span class="section-tag">✈ EXPLORE THE WORLD</span>
  <h1>All Traveling<br><em>Packages</em></h1>
  <p>সকল ট্র্যাভেল প্যাকেজ — Discover your perfect destination from our full collection.</p>
</div>

<!-- ================= PACKAGES GRID ================= -->
<main>
  <div class="container" id="packagesGrid">
    <% if (packages && packages.length > 0) { %>
      <% packages.forEach(pkg => { %>
        <%- include('partials/card', { pkg: pkg, type: 'travel' }) %>
      <% }); %>
    <% } else { %>
      <div class="no-packages">
        <p>No travel packages available at the moment.</p>
      </div>
    <% } %>
  </div>
</main>

<%- include('partials/footer') %>

<style>
  .page-hero {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-align: center;
    padding: 80px 20px 60px;
  }
  .page-hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: 3rem;
    margin: 20px 0 10px;
  }
  .page-hero p {
    font-size: 1.2rem;
    opacity: 0.9;
    max-width: 600px;
    margin: 0 auto;
  }
  main {
    padding: 60px 20px;
  }
  .container {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 30px;
  }
  .no-packages {
    grid-column: 1 / -1;
    text-align: center;
    padding: 60px;
    font-size: 1.2rem;
    color: #666;
  }
</style>

</body>
</html>
