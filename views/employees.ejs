<%- include('partials/header', { pageTitle: 'Our Team — Nusrat International', activePage: 'about', bodyClass: '' }) %>

<!-- PAGE HERO -->
<section class="page-hero team-hero">
  <div class="hero-bg-overlay"></div>
  <div class="hero-content-wrap">
    <span class="section-tag">👥 OUR PEOPLE</span>
    <h1>Meet Our <em>Team</em></h1>
    <p>The dedicated professionals behind every journey, placement, and pilgrimage.</p>
  </div>
</section>

<!-- FOUNDER'S MESSAGE SECTION -->
<section class="founder-message-section">
  <div class="container">
    <div class="founder-header">
      <span class="section-tag-small">FOUNDER'S MESSAGE</span>
      <h2>A Message from Our Founder</h2>
    </div>
    
    <% if (founder) { %>
    <div class="founder-content-wrapper">
      <div class="founder-profile-card">
        <div class="founder-image-wrapper">
          <% if (founder.photo?.url) { %>
            <img src="<%= founder.photo.url %>" alt="<%= founder.name?.en %>" class="founder-img" style="width: 160px; height: 160px; border-radius: 50%; object-fit: cover;">
          <% } else { %>
            <div class="founder-initials">FND</div>
          <% } %>
        </div>
        <div class="founder-info">
          <h3 class="founder-name"><%= founder.name?.en || founder.name || 'Founder' %></h3>
          <p class="founder-title"><%= founder.designation?.en || founder.designation || 'Proprietor & Founder' %></p>
        </div>
      </div>

      <div class="founder-bio">
        <% 
            const bioText = founder.bio?.en || founder.bio || '';
            if (bioText) {
              bioText.split('\n').filter(p => p.trim()).forEach(paragraph => {
        %>
            <p><%= paragraph %></p>
          <% }) %>
        <% } %>
        
        <% if (founder.quote?.en || founder.quote) { %>
        <div class="founder-quote">
          "<%= founder.quote?.en || founder.quote %>"
        </div>
        <% } %>
      </div>
    </div>
    <% } %>
  </div>
</section>

<!-- EMPLOYEES -->
<main class="container">
  <div class="team-wrap">
    <div class="employees">
      <% if (typeof employees !== 'undefined' && employees.length > 0) { %>
        <% employees.forEach(emp => { %>
          <div class="employee-card">
            <div class="emp-image-wrap">
              <img src="<%= emp.photo?.url || emp.image || emp.photo || 'https://picsum.photos/300/300' %>" alt="<%= emp.name?.en || emp.name %>">
            </div>
            <div class="emp-info">
              <h3 class="emp-name"><%= emp.name?.en || emp.name %></h3>
              <p class="emp-position"><%= emp.designation?.en || emp.position || emp.designation %></p>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <!-- Default employees -->
        <div class="employee-card">
          <div class="emp-image-wrap">
            <img src="https://picsum.photos/300/300?1" alt="Md. Nusrat Ullah">
          </div>
          <div class="emp-info">
            <h3 class="emp-name">Md. Nusrat Ullah</h3>
            <p class="emp-position">Managing Director</p>
          </div>
        </div>
        <div class="employee-card">
          <div class="emp-image-wrap">
            <img src="https://picsum.photos/300/300?2" alt="Karim Hossain">
          </div>
          <div class="emp-info">
            <h3 class="emp-name">Karim Hossain</h3>
            <p class="emp-position">Travel Manager</p>
          </div>
        </div>
        <div class="employee-card">
          <div class="emp-image-wrap">
            <img src="https://picsum.photos/300/300?3" alt="Sultana Parvin">
          </div>
          <div class="emp-info">
            <h3 class="emp-name">Sultana Parvin</h3>
            <p class="emp-position">Ticketing Officer</p>
          </div>
        </div>
        <div class="employee-card">
          <div class="emp-image-wrap">
            <img src="https://picsum.photos/300/300?4" alt="Farhan Ahmed">
          </div>
          <div class="emp-info">
            <h3 class="emp-name">Farhan Ahmed</h3>
            <p class="emp-position">Travel Consultant</p>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</main>

<%- include('partials/footer') %>
