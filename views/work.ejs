<%- include('partials/header', { title: 'All Work Opportunities - Nusrat International', activePage: 'work' }) %>

<!-- ================= PAGE HERO ================= -->
<div class="page-hero">
  <span class="section-tag">💼 WORK ABROAD</span>
  <h1>All Work<br><em>Opportunities</em></h1>
  <p>সকল কাজের সুযোগ — Legitimate work visa opportunities in Gulf countries.</p>
</div>

<!-- ================= PACKAGES GRID ================= -->
<main>
  <div class="container" id="packagesGrid">
    <% if (packages && packages.length > 0) { %>
      <% packages.forEach(pkg => { %>
        <%- include('partials/card', { pkg: pkg, type: 'work' }) %>
      <% }); %>
    <% } else { %>
      <div class="no-packages">
        <p>No work opportunities available at the moment.</p>
      </div>
    <% } %>
  </div>
</main>

<%- include('partials/footer') %>

<style>
  .page-hero {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    color: white;
    text-align: center;
    padding: 80px 20px 60px;
  }
  .page-hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: 3rem;
    margin: 20px 0 10px;
  }
  .page-hero p {
    font-size: 1.2rem;
    opacity: 0.9;
    max-width: 600px;
    margin: 0 auto;
  }
  main {
    padding: 60px 20px;
  }
  .container {
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 30px;
  }
  .no-packages {
    grid-column: 1 / -1;
    text-align: center;
    padding: 60px;
    font-size: 1.2rem;
    color: #666;
  }
</style>

</body>
</html>
